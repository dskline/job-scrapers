schema: http://localhost:8080/v1/graphql
generates:
  src/features/integrations/graphql/types.ts:
    plugins:
      - typescript
    config:
      namingConvention:
        transformUnderscore: true
